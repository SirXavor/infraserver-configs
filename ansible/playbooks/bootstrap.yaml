---
- name: Bootstrap localhost
  hosts: localhost
  connection: local
  become: true
  gather_facts: true

  tasks:
    - name: Ejecutar roles definidos
      include_role:
        name: "{{ item }}"
      loop: "{{ automation.roles | default([]) }}"