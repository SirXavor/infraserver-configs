---
- name: Crear fichero de hardening sysctl
  copy:
    dest: /etc/sysctl.d/99-ccn-hardening.conf
    owner: root
    group: root
    mode: "0644"
    content: |
      # Managed by Ansible - CCN kernel/network hardening

      # ----------------------------
      # Red
      # ----------------------------
      net.ipv4.conf.all.send_redirects = 0
      net.ipv4.conf.default.send_redirects = 0
      net.ipv4.conf.all.accept_redirects = 0
      net.ipv4.conf.default.accept_redirects = 0
      net.ipv4.conf.all.secure_redirects = 0
      net.ipv4.conf.default.secure_redirects = 0

      net.ipv4.conf.all.log_martians = 1
      net.ipv4.conf.default.log_martians = 1

      net.ipv4.icmp_ignore_bogus_error_responses = 1
      net.ipv4.icmp_echo_ignore_broadcasts = 1

      net.ipv4.tcp_syncookies = 1

      # En nodos edge normalmente no deben rutear tráfico a nivel host
      net.ipv4.ip_forward = {{ sysctl_ipv4_ip_forward | default(0) }}

      # ----------------------------
      # Sistema de ficheros / memoria
      # ----------------------------
      fs.suid_dumpable = 0
      fs.protected_symlinks = 1
      fs.protected_hardlinks = 1

      # ----------------------------
      # Kernel
      # ----------------------------
      kernel.panic = 10
      kernel.kexec_load_disabled = 1
      kernel.dmesg_restrict = 1
      kernel.randomize_va_space = 2
      kernel.kptr_restrict = 2
    backup: true

- name: Aplicar configuración sysctl
  command: sysctl --system
  register: sysctl_apply
  changed_when: false